# 兼职打卡系统 — 部署指南（Supabase 版）

## 功能地址

| 功能 | 说明 | 地址 |
|------|------|------|
| **员工打卡** | 手机扫码签到/签退 | `https://lingdianwu.github.io/checkin/index.html?store=门店ID` |
| **管理后台** | PC 端考勤管理 | `https://lingdianwu.github.io/checkin/admin.html` |
| **GPS 采集** | 手机端采集门店坐标 | `https://lingdianwu.github.io/checkin/location.html` |
| **本地测试** | 本机开发调试 | `http://127.0.0.1:8080/admin.html` |

> 管理后台初始密码：`admin123`，超级管理员登录时不填手机号。

### 带门店参数的打卡链接

| 门店 | 打卡地址 |
|------|----------|
| 天津门店 | `https://lingdianwu.github.io/checkin/index.html?store=tianjin` |
| 南京门店 | `https://lingdianwu.github.io/checkin/index.html?store=nanjing` |
| 西安门店 | `https://lingdianwu.github.io/checkin/index.html?store=xian` |

> 替换 `?store=` 后面的门店ID即可对应不同门店。二维码可在管理后台「门店二维码」标签页获取，含每日动态安全码。

---

## 第一步：注册 Supabase

1. 打开 https://supabase.com 注册账号（用 GitHub 或邮箱）
2. 创建一个新项目（Organization 随意、Project 取名如 `checkin`）
3. Database Password 设置一个强密码（记下来，后续 SQL Editor 会用到）
4. Region 选择 **Northeast Asia (Tokyo)** 或 **Southeast Asia (Singapore)**，离国内最近
5. 等 1-2 分钟项目创建完成

## 第二步：获取 API 密钥

1. 进入项目 → Settings → API
2. 复制 **Project URL**（形如 `https://xxxxxxxxxxxx.supabase.co`）
3. 复制 **anon public key**（很长一串）
4. 打开本项目的 `js/config.js`，填入：

```js
var SUPABASE_CONFIG = {
  url: 'https://xxxxxxxxxxxx.supabase.co',
  anonKey: 'eyJhbGciOiJI...你的完整key'
};
```

## 第三步：创建数据表

1. Supabase 控制台 → SQL Editor → 新建查询
2. 把本项目的 `setup.sql` 内容全部粘贴进去
3. 点击 Run 执行
4. 返回 Table Editor，确认 `store`、`roster`、`check_record`、`app_config` 四个表已创建，且 `store` 表里已有三条门店数据

> 如果门店不同，修改 `setup.sql` 末尾的 INSERT 语句再执行。

## 第四步：部署前端

**方式一：GitHub Pages（推荐，免费）**

1. 在 GitHub 新建仓库（如 `checkin`）
2. 把本项目的 `index.html`、`admin.html`、`css/`、`js/` 推送到仓库（不要提交 `setup.sql` 和 `部署指南.md`）

```
git init
git add index.html admin.html css/ js/
git commit -m "init"
git remote add origin https://github.com/你的用户名/checkin.git
git push -u origin main
```

3. GitHub 仓库 → Settings → Pages → Source 选 `main` → Save
4. 一两分钟后页面生效：`https://你的用户名.github.io/checkin/`

**方式二：Vercel（备选）**

1. 打开 https://vercel.com 注册（用 GitHub 登录）
2. New Project → 导入仓库 → 直接 Deploy

## 第五步：初始化配置

1. 打开管理后台，用 `admin123` 登录 → 立即修改密码
2. 在"门店管理"中确认/新增门店（门店ID用英文，如 `beijing`）
3. 在"花名册管理"中添加各门店员工，**务必录入手机号**（用于防代打卡验证）
4. 切换到"门店二维码"标签，二维码已自动生成
5. 截图打印，贴在各门店入口

## 使用方式

- **员工首次打卡**：手机扫码 → 选自己名字 → 输入手机号验证 → 到岗签到
- **员工再次打卡**：再次扫码 → 自动识别身份 → 直接签到/签退
- **老板查看**：打开管理后台 → 选择日期/门店 → 查看所有人在岗时长
- **新增门店**：管理后台 → 门店管理 → 输入门店ID和名称 → 添加

## 防代打卡说明

- 花名册中录入了手机号的员工，打卡时必须输入正确手机号验证
- 手机号不匹配无法打卡，有效防止代替打卡
- 验证通过后浏览器自动记住，同一手机下次无需再输
- 如果员工未录手机号，则跳过验证（兼容老数据）

## 常见问题

**Q: 员工换手机了怎么办？**
A: 扫码后重新选名字 → 输入手机号验证即可，新手机自动记住。

**Q: 忘记管理密码怎么办？**
A: 登录 Supabase 控制台 → Table Editor → app_config 表 → 修改 admin_password 字段。

**Q: 免费额度够用吗？**
A: Supabase 免费版包含 500MB 数据库、每月 50,000 月活用户、无限 API 请求。3 个门店一天不到 100 次请求，完全够用。

**Q: 国内访问慢怎么办？**
A: Supabase 东京节点（Northeast Asia）在国内访问速度尚可，Ping 延迟 50-100ms。如果特别在意速度，可以给 GitHub Pages 绑定一个国内 CDN 加速的自定义域名。
